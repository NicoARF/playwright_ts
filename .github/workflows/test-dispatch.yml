name: Playwright Tests Dispatch
on:
  repository_dispatch:
    types: [run-tests]
jobs:
  test:
    timeout-minutes: 60
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-node@v4
      with:
        node-version: lts/*
    - name: Print PR Information
      run: |
        echo "Event Name: ${{ github.event.client_payload.event_name }}"
        echo "Repository: ${{ github.event.client_payload.repository }}"
        echo "Event Type: ${{ github.event.client_payload.event_details }}"
    - name: Install dependencies
      run: npm ci
    - name: Install Playwright Browsers
      run: npx playwright install --with-deps
    - name: Run Playwright tests
      run: |
        echo "Running tests for: ${{ github.event.client_payload.event_name }}"
        npx playwright test
    - uses: actions/upload-artifact@v4
      if: ${{ !cancelled() }}
      with:
        name: playwright-report
        path: playwright-report/
        retention-days: 30
